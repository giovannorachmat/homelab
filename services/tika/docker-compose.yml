services:
  tika:
    container_name: tika
    image: apache/tika:4.0.0
    restart: unless-stopped

    ports:
      - "9998:9998"

    volumes:
      - ../../runtime/tika/data:/data

    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo >/dev/tcp/localhost/9998' || exit 1"]
      interval: 1m30s
      timeout: 10s
      retries: 5
      start_period: 10s

    networks:
      - private-net

    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 4G
        reservations:
          cpus: "1"
          memory: 2G

networks:
  private-net:
    external: true
